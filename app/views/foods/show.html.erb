<br />
<div class="booyah-box col-10 offset-1">

	<% FoodType.all.each do |food_type| %>
		<%= food_type.name %> 
		<% food_type.foods.each do |food| %>
		  <%= food.name %>
		<% end %>
	<% end %>

</div>